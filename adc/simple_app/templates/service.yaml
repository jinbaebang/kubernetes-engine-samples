apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple_app.fullname" . }}
  labels:
    {{- include "simple_app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simple_app.selectorLabels" . | nindent 4 }}
